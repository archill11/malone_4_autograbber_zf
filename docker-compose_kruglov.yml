services:
  app:
    container_name: grabber-app_kruglov
    build: ./
    restart: unless-stopped
    ports:
      - 4025:4025
    environment:
      BOT_PREFIX: _kruglov
      TG_ENDPOINT: https://api.telegram.org/bot%s/%s
      APP_PORT: 4025
      PG_USER: postgres
      PG_PASSWORD: secret
      PG_DATABASE: grabber
      PG_HOST: grab_db_kruglov
      PG_PORT: 5432
    volumes:
      - ./volumes/logs:/app/logs
      - ./volumes/files:/app/files
    networks:
      - malone_4

  db:
    container_name: grab_db_kruglov
    image: postgres:15-alpine
    ports:
      - "5457:5432"
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: grabber
    networks:
      - malone_4
    volumes:
      - ./volumes/db/data:/var/lib/postgresql/data

networks:
  malone_4:
    external: true